
import 'package:flutter/material.dart';

class AppColors {
  // Main Colors
  static const primaryColor = Color(0xff3B5EDF);

  // Light Mode Colors
  static const lightBackgroundColor = Color(0xffEEEEEE);
  static const lightContainerColor = Colors.white;

  // Dark Mode Color
  static const Color darkBackgroundColor = Color.fromRGBO(28, 28, 28, 1);
  static const Color darkContainerColor = Color(0xFF080808);

  // Other Custom Colors
  static const red = Color(0xffD02026);
  static const green = Color(0xff1DB68B);
  static const grey = Color(0xff858585);
}